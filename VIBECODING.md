# Worktime App VibeCoding 文档

版本：v1.0  
日期：2026-04-14  
适用范围：`worktime`（Android 离线倒班 App）

## 1. 文档目标
本文件用于统一“你 + AI 助手”的开发方式，让需求快速落地、质量可控、节奏稳定。

## 2. 产品北极星
一句话目标：用户输入倒班参数后，App 能准确生成周期排班，并一键创建上班开始闹钟。

MVP 范围（冻结）：
1. 仅保留 1 个倒班方案。
2. 仅创建“上班开始”闹钟。
3. 支持“提前 X 分钟提醒”。
4. 日历视图为主。
5. 完全离线，不联网。

## 3. 功能定义（必须满足）
1. 参数输入  
`总时长(天)`、`工作时长`、`休息时长`、`首次工作开始时间`、`提前提醒分钟`。
2. 排班计算  
从首次工作开始，按“工作-休息”循环，直到总时长结束。
3. 闹钟创建  
只创建未来的“上班开始提醒”。
4. 闹钟重建  
参数变更后支持“先清理旧闹钟，再重建”。
5. 本地持久化  
方案、排班结果、闹钟映射本地保存。

## 4. 非功能约束
1. UI 语言：100% 中文。
2. 网络：禁止依赖任何在线接口。
3. 时间计算：统一使用 `java.time`。
4. 兼容性：至少覆盖 Android 8.0+（API 26+）。
5. 可测试性：核心排班逻辑必须单元测试覆盖。

## 5. 技术基线
1. 语言与框架：`Kotlin + Jetpack Compose`。
2. 架构：`UI / Domain / Data` 分层。
3. 状态管理：`ViewModel + StateFlow`。
4. 存储：`Room`。
5. 闹钟：`AlarmManager + BroadcastReceiver`。
6. 重启恢复：`BOOT_COMPLETED` 后重建闹钟。

## 6. 目录建议
```text
app/src/main/java/com/example/worktime/
  ui/
    home/
    editor/
    calendar/
    settings/
  domain/
    model/
    usecase/
    scheduler/
  data/
    local/
      db/
      dao/
      entity/
    repository/
  alarm/
    receiver/
    manager/
```

## 7. VibeCoding 工作流
每个功能都按以下节奏执行：

1. 明确输入  
写清楚“用户目标 + 边界 + 验收标准”。
2. 让 AI 产出第一版  
先要最小可运行实现，不先追求完美。
3. 人工做三类校验  
业务正确性、边界条件、Android 版本兼容。
4. 补测试  
先补失败用例，再修实现直到通过。
5. 小步提交  
每次提交只做一件事，避免大杂烩。

## 8. AI 提示词模板（可直接复用）
### 8.1 新功能模板
```text
你是 Android 高级工程师。请在现有项目中实现以下功能：
【功能】<功能名>
【输入】<参数>
【规则】<业务规则>
【边界】<异常/极值/跨天等>
【输出】给出可运行代码、涉及文件列表、必要测试。
要求：Kotlin、Compose、中文文案、离线可用。
```

### 8.2 Bug 修复模板
```text
请定位并修复以下问题：
【现象】<用户看到的错误>
【复现步骤】1...2...3...
【期望结果】<正确行为>
要求：先给根因，再给最小修复补丁，并补回归测试。
```

### 8.3 重构模板
```text
请对以下模块做不改变行为的重构：
【模块】<文件/包名>
【目标】可读性/可测试性/解耦
要求：接口不变，补充必要注释和测试，给出风险点。
```

## 9. 验收清单（DoD）
1. 功能可跑通：设置参数 -> 生成排班 -> 创建闹钟。
2. 边界正确：跨天、跨月、跨年、提前提醒为 0。
3. 修改方案后旧闹钟不残留。
4. 飞行模式下完整可用。
5. 关键用例测试通过。
6. 无明显中文文案错误。

## 10. 测试最小集合
1. `总时长=1天` 的最小周期。
2. `工作时长 > 休息时长`。
3. `休息时长 > 工作时长`。
4. 首次开始时间在过去，闹钟只建未来。
5. 提前提醒导致时间跨到前一天。
6. 参数更新后重建闹钟数量正确。

## 11. 提交规范
提交信息建议：
1. `feat: 新增排班周期计算引擎`
2. `feat: 支持上班开始闹钟一键创建`
3. `fix: 修复跨天提前提醒时间计算错误`
4. `test: 补充排班边界条件单元测试`
5. `refactor: 拆分排班计算与闹钟调度职责`

## 12. 当前开发顺序（建议）
1. 先做 `domain/scheduler` 纯计算模块 + 单测。
2. 再做方案编辑页与本地存储。
3. 接着接入日历展示。
4. 最后做闹钟创建、清理、重建、重启恢复。

---
如果后续需求变化，先更新本文件第 2、3、9 章，再开始写代码，避免实现偏航。
