# Worktime App PRD（MVP）

版本：v1.0  
日期：2026-04-14  
状态：可开发

## 1. 产品概述
Worktime 是一个离线安卓倒班工具。用户设置轮班参数后，系统自动生成排班并一键创建“上班开始”闹钟。

目标用户：有固定工作/休息间隔的倒班人群。  
核心价值：减少手动算班与手动设闹钟的时间成本。

## 2. MVP 范围
### 2.1 In Scope
1. 单一倒班方案（仅保留一个，覆盖保存）。
2. 参数输入：总时长（天）、工作时长、休息时长、首次工作开始时间、提前提醒分钟。
3. 自动生成排班（日历为主展示）。
4. 一键创建未来“上班开始”闹钟。
5. 修改方案后一键重建闹钟（先清理再创建）。
6. 完全离线可用。

### 2.2 Out of Scope
1. 多方案管理。
2. 云同步、账号系统。
3. 联网节假日/日历服务。
4. 复杂班次模板市场。

## 3. 用户故事
1. 作为倒班用户，我希望输入固定周期参数后自动得到完整时间表。
2. 作为倒班用户，我希望一键创建提醒，避免逐个设闹钟。
3. 作为倒班用户，我希望看到月历上哪些天有上班开始事件。
4. 作为倒班用户，我希望改参数后可以直接重建闹钟，不留旧数据。

## 4. 功能需求
## 4.1 方案设置
字段：
1. `totalDays`：本次轮班总时长，单位天，`> 0`。
2. `workDurationMinutes`：工作时长，单位分钟，`> 0`。
3. `restDurationMinutes`：休息时长，单位分钟，`> 0`。
4. `firstWorkStartAt`：首次工作开始时间，本地时区时间。
5. `remindBeforeMinutes`：提前提醒分钟，`>= 0`。

行为：
1. 输入非法时禁止保存并给中文错误提示。
2. 保存后立即刷新首页摘要和日历。

## 4.2 排班计算
规则：
1. 周期 = `工作时长 + 休息时长`。
2. 从 `firstWorkStartAt` 开始，交替生成工作段和休息段。
3. 截止时间 = `firstWorkStartAt + totalDays`。
4. 仅“工作段开始时刻”可用于闹钟创建。

边界：
1. 跨天、跨月、跨年必须正确。
2. `firstWorkStartAt` 可早于当前时间。
3. 闹钟创建时只取“未来时刻”。

## 4.3 日历展示（主视图）
1. 月历展示当月班次信息。
2. 有“工作开始”事件的日期做高亮标记。
3. 点击某天展示当天事件列表（开始时间、提醒时间、状态）。

## 4.4 闹钟管理
1. 一键创建：创建全部未来工作开始提醒。
2. 一键重建：删除旧闹钟并按当前方案重建。
3. 仅创建“上班开始提醒”，不创建结束提醒。
4. 显示闹钟统计：已创建数量、下一次提醒时间。

## 5. 页面与流程
## 5.1 页面列表
1. 首页：方案摘要、下次上班倒计时、快捷按钮。
2. 方案编辑页：参数输入、校验、保存。
3. 日历页：月历 + 当日明细。
4. 设置页：默认提前提醒分钟（可选轻配置）。

## 5.2 主流程
1. 首次进入 -> 无方案提示 -> 去设置方案。
2. 设置参数并保存 -> 生成排班 -> 返回首页。
3. 点击“一键创建闹钟” -> 创建未来闹钟 -> 成功提示。
4. 修改参数 -> 点击“重建闹钟” -> 旧闹钟清理 + 新闹钟写入。

## 6. 数据结构（字段级）
## 6.1 ShiftPlan（单行记录）
1. `id: Long`（固定为 1）
2. `total_days: Int`
3. `work_duration_minutes: Int`
4. `rest_duration_minutes: Int`
5. `first_work_start_at_epoch_millis: Long`
6. `remind_before_minutes: Int`
7. `updated_at_epoch_millis: Long`

## 6.2 ShiftEvent
1. `id: Long`
2. `plan_id: Long`
3. `type: String`（`WORK`/`REST`）
4. `start_at_epoch_millis: Long`
5. `end_at_epoch_millis: Long`
6. `alarm_trigger_at_epoch_millis: Long?`（仅 WORK 有值）

## 6.3 AlarmMapping
1. `id: Long`
2. `plan_id: Long`
3. `event_id: Long`
4. `request_code: Int`（PendingIntent 唯一标识）
5. `trigger_at_epoch_millis: Long`
6. `created_at_epoch_millis: Long`

## 7. 权限与系统能力
1. 通知权限（Android 13+）。
2. 精确闹钟能力按系统版本策略处理。
3. 开机广播（`BOOT_COMPLETED`）用于恢复闹钟。

## 8. 非功能需求
1. 全中文文案。
2. 无联网依赖，飞行模式可用。
3. 核心计算纯函数化，可单测。
4. 关键操作失败有明确中文提示。

## 9. 验收标准（DoD）
1. 可完整走通：配置 -> 生成 -> 创建闹钟 -> 到点提醒。
2. 修改方案后旧闹钟不残留。
3. 仅创建未来闹钟，不创建过去提醒。
4. 日历展示与计算结果一致。
5. 重启后可恢复既有闹钟（若系统允许）。

## 10. 测试用例（MVP 最小集）
1. `totalDays=1`，最小可运行。
2. `work > rest` 与 `rest > work` 两类周期。
3. 首次开始时间在过去，确保只建未来闹钟。
4. 提前提醒跨到前一天。
5. 修改参数后重建闹钟数量和时间正确。
6. 跨月/跨年排班结果正确。

## 11. 里程碑（建议）
1. M1：排班计算引擎 + 单测。
2. M2：方案编辑 + 本地存储。
3. M3：日历展示。
4. M4：闹钟创建/重建/恢复 + 联调验收。

